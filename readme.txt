1. create database named "picture";
2. run the script below to create the table pic:

CREATE TABLE pic
(
	id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    image VARCHAR(100) NOT NULL
)